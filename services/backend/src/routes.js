const express = require('express');
const bodyParser = require('body-parser');
// const users = require('../src/modules/users/users');

const router = express.Router();

// router.use('/users', users);

module.exports = router;
